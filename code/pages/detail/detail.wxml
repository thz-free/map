<!--pages/detail/detail.wxml-->
<view class="container">
  <view class="content">{{content}}</view>
  <button wx:if='{{isAdd}}' plain bindtap='cancelFavorites'>取消接单</button>
  <button wx:else plain bindtap='addFavorites'>接单</button>
</view>
